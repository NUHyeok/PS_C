#include <stdio.h>

typedef struct Print_queue{
    int num;
    int importance;
    struct Print_queue *next;
}Print_queue;

Print_queue queue[100];

int main(void){
    int T;
    scanf("%d", &T);

    for(int i = 0; i < T; i++){
        int N, M;
        for(int j = 0; j < N; j++){
            scanf("%d")
        }
    }
}